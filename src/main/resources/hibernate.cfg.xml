<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class"> org.apache.derby.jdbc.ClientDriver</property>
        <property name="connection.url"> jdbc:derby://localhost:1527/DerbyDB </property>
        <property name="connection.username">APP</property>
        <property name="connection.password">APP</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.DerbyDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <!--<property name="show_sql">true</property>-->
        <property name="hibernate.show_sql">false</property>

        <!-- Names the annotated entity class -->
        <mapping class="Entities.PersonEntity"/>
        <mapping class="Entities.ChatEntity"/>
        <mapping class="Entities.ChatmemberEntity"/>
        <mapping class="Entities.MessageEntity"/>
        <mapping class="Entities.ReservationEntity"/>
        <mapping class="Entities.ReparationsEntity"/>
        <mapping class="Entities.InstrumentEntity"/>
        <mapping class="Entities.InstrumentPicturesEntity"/>

    </session-factory>
</hibernate-configuration>